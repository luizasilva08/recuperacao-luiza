version: '3.9'

services:
  mysql:
    image: mysql:8.0
    container_name: ----------
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: ----------
      MYSQL_DATABASE: ----------
      MYSQL_USER: ----------
      MYSQL_PASSWORD: ----------
    ports:
      - "3306:3306"
    volumes:
      - dados_mysql:/var/lib/mysql
      - ./scripts:/docker-entrypoint-initdb.d
    networks:
      - rede_techfactory

volumes:
  dados_mysql:

networks:
  rede_techfactory:
    driver: bridge